# 2026-08-27

## ziwei-doushu 接入 iztro 官方 API（付费解读落地）

目标：把 ziwei-doushu 的 AI 解读换成 iztro 官方托管模型（更专业），做成收费产品底座。

### 联调实测关键结论（sk_ziwei_ Key 已验证）
- **`role:"system"` 消息生效**：可注入天纪体系/格局库（测试返回纯 emoji 证明强控制）。
- **`metadata.system_prompt_override` 被忽略**；Session API 的 `instructions` 字段也被静默丢弃。→ 注入规则只能用 system 角色，不能用 override。
- **SSE 是真正逐 token 流式**（`delta.content`），非整段；较早"整段返回"是 Python SDK demo 缓冲问题。
- 响应带 `billing.balance_after_cny`（每次返回余额，省去查控制台）。
- 单次短问 `prompt_tokens≈12862`（命盘被注入 prompt），成本≈¥0.015/次。
- 必须携带**阳历生日+时辰+性别**，否则 iztro-mingpan 无法计算、模型要求补信息（原 buildChartContext 缺阳历生日，已补）。

### 改动文件
- `lib/iztro.ts`（新增）：官方客户端 `streamIztroChat`/`iztroChat`，OpenAI 兼容 SSE，自动 language=zh，不发送 temperature。
- `lib/llm.ts`：新增 `iztro` provider（读 IZTRO_API_KEY/BASE/MODEL），`streamChatCompletion` 对 iztro 委派给 lib/iztro.ts。
- `app/api/interpret/route.ts`：`buildChartContext` 增加阳历生日行（触发 mingpan）；现有路由/SSE 转换/多轮零改动即生效。
- `.env.local`：`AI_PROVIDER=iztro,agnes`（iztro 首选、agnes 兜底），写入 IZTRO_API_KEY。

### 验证
- `npx tsc --noEmit` 通过（exit 0）。
- 临时脚本镜像 streamIztroChat 跑通：HTTP 200，流式返回 1057 字正经紫微解读，system 提示生效。

### 未完成 / 后续
- **免费草稿 vs 付费深度**的 UI 分层（freemium 按钮/配额）未做——目前是全量走 iztro。用户管理/订阅/配额表未建。
- ⚠️ Key 已出现在聊天中，建议去 platform.iztro.com/console 轮换一次。
- 奇门（iztro-qimen-v3）路径未接，lib/iztro.ts 已预留 model/currentDatetime 参数。
