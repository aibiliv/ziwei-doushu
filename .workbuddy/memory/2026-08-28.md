# 2026-08-28

## iztro 测通后切回 agnes 免费 + 落地免费/付费分层
- 按用户指示：iztro 已联调通过，先切回 agnes 作免费默认（AI_PROVIDER=agnes,iztro），继续做收费分层。
- 新增 `lib/plan.ts`（文件存储 MVP 配额/订阅）、`app/api/plan/route.ts`（MVP 激活）、改造 `app/api/interpret/route.ts`（plan 路由 + 402 闸门）、`components/InsightPanel.tsx`（深度解读开关 + locked 主题接 deep + 402 升级弹窗，移除 DEV_BYPASS_LOCKED）。
- 验证：npx tsc --noEmit 通过；lib/plan.ts 算法用 node --experimental-strip-types 实测通过。
- 待办（用户已知）：真实鉴权(NextAuth)+支付(Stripe/微信)+Postgres 三表 替换 lib/plan.ts 文件存储；控制台轮换 iztro Key。

## 按截图重新拆分为「命盘分析 / AI 对话」两个栏目
- 用户要求：免费只给 6 个维度解读、所有维度不提供 AI 对话；AI 对话单独开栏目且收费。
- 改造 `components/InsightPanel.tsx`：顶部「命盘分析 / AI 对话」切换；命盘分析 tab 保留 6 免费 + 7 locked 维度，去掉底部追问输入框；AI 对话 tab 独立多轮聊天，未订阅时展示升级提示。
- 后端：
  - 抽离 `lib/prompt.ts`（buildSystemPrompt / buildChartContext）、`lib/sse.ts`（SSE_HEADERS / buildTransformStream）。
  - 新增 `app/api/chat/route.ts`：iztro 流式 + 服务端维护 chatHistory（最近 20 条）+ 每次调用扣 deep 额度，未订阅返回 402。
  - 扩展 `lib/plan.ts`：PlanRecord 增加 `chatHistory` 与 `chatSessionId`，新增 getChatHistory / appendChatHistory / clearChatHistory。
  - `app/api/interpret/route.ts` 复用 lib/prompt 与 lib/sse；逻辑不变：6 免费维度走 agnes（free），7 locked 维度走 iztro（deep）并需订阅。
- 验证：tsc 通过；本地 dev 测试：/api/chat 免费用户 402、pro 用户真实流式返回正常；/api/interpret deep 流式正常。
